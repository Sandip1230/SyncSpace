# SyncSpace
Real-time collaborative whiteboard &amp; code editor built on the MERN stack. Multiple users can draw and code simultaneously using WebSockets (Socket.io) and CRDTs (Yjs) for conflict-free sync — no race conditions, no overwritten edits.
